apply plugin: 'com.google.gms.google-services' // Solo si usas Firebase

android {
    compileSdkVersion 34
    buildToolsVersion "34.0.0"

    defaultConfig {
        applicationId "org.nativescript.XEventsClient" // Asegúrate de que este sea tu applicationId correcto
        minSdkVersion 23
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"

        generatedDensities = []
    }

    aaptOptions {
        additionalParameters "--no-version-vectors"
    }
}
